package com.cisc181.core;

import java.util.Date;
import java.util.UUID;

public class Section {

	private final UUID courseID = new UUID(1 , 1);
	private final UUID semesterID = new UUID(1 , 1);
	private final UUID sectionID = new UUID(1 , 1);
	private int roomId;
}
